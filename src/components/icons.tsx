import { siteConfig } from "@/config/site";
import { XIcon, Loader2Icon, MenuIcon } from "lucide-react";

export const Icons = {
  logo: () => <h1 className="font-light">Xal</h1>,
  close: XIcon,
  loader: Loader2Icon,
  menu: MenuIcon,
};
